SET SQL_MODE="NO_AUTO_VALUE_ON_ZERO";

CREATE TABLE IF NOT EXISTS `` (
   `name` VARCHAR(30),
   `address` VARCHAR(100),
   `post_code` INT(4),
   `phone_number` INT(10),
   `product supplied` VARCHAR(50)
   PRIMARY KEY (``)
) ENGINE=MyISAM DEFAULT CHARSET=latin1;

INSERT INTO `` () VALUES
();